package com.example.commitech.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================
// 🌞 Light Theme Colors
// ==========================

// Warna utama (Teal)
val TealLight = Color(0xFF009688)       // Warna utama terang
val TealDark = Color(0xFF00695C)        // Versi lebih gelap
val TealOn = Color(0xFFFFFFFF)          // Warna teks di atas teal

// Background & Surface
val TealBackground = Color(0xFFF5F7FA)  // Latar belakang abu lembut
val TealOnBackground = Color(0xFF1C1B1F) // Warna teks utama
val TealSurface = Color(0xFFFFFFFF)     // Permukaan (card, konten)
val TealOnSurface = Color(0xFF1C1B1F)   // Teks di permukaan

val TealSurfaceDark = Color(0xFF2B2930)     // Untuk transisi ke dark mode
val TealOnSurfaceDark = Color(0xFFE6E1E5)

// Secondary (Biru)
val BlueSecondary = Color(0xFF0277BD)             // Biru aksen
val BlueSecondaryContainer = Color(0xFFB3E5FC)    // Biru muda container
val BlueOnSecondaryContainer = Color(0xFF003C8F)  // Tulisan di atas biru muda

// Primary container (Teal muda)
val TealPrimaryContainer = Color(0xFFB2DFDB)      // Teal muda container
val TealOnPrimaryContainer = Color(0xFF004D40)    // Tulisan di atas teal muda


// ==========================
// 🌙 Dark Theme Colors
// ==========================

val TealDarkBackground = Color(0xFF121212)
val TealDarkOnBackground = Color(0xFFE6E1E5)

val TealDarkPrimaryContainer = Color(0xFF004D40)   // Teal tua untuk dark mode
val TealDarkOnPrimaryContainer = Color(0xFF80CBC4) // Tulisan teal muda

val BlueDarkSecondaryContainer = Color(0xFF174872)
val BlueDarkOnSecondaryContainer = Color(0xFF90CAF9)
